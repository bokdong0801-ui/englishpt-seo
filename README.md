# ENGLISH PT SEO

Production repository for the ENGLISH PT static SEO site.

## Current release

- 95 production content pages
- 26-page expansion included
- 74 legacy redirects retained
- Shared CSS/JS in `assets/`
- `sitemap.xml`, `robots.txt`, `_redirects`, and `vercel.json` included
- Current canonical/sitemap domain: `https://englishpt.kr`

Before public indexing, confirm the final domain and deploy this repository through a host that supports server-side permanent redirects. Vercel is the recommended current path because `vercel.json` is already included.
